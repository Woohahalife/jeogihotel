## swagger api ??
springdoc:
  api-docs:
    version: openapi_3_0 # Open API ?? ??
    path: /public-api/api-docs # api ?? ??
  default-consumes-media-type: application/json # ?? ?? json?? ??
  default-produces-media-type: application/json # ?? ?? json?? ??
  swagger-ui: # ui ??
    operations-sorter: alpha # ???? ?? ??
    tags-sorter: alpha # ???? ?? ??
    path: /public-api/swagger-ui # ui uri ?? : localhost:8080//public-api/swagger-ui
    disable-swagger-default-url: true # ?? uri ????
    display-request-duration: true # ?? ???? ui? ??
  paths-to-match: # ui? ?? api ?? ??
    - /**
